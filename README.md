# Diligent Life

운동 경로·속도·몸무게의 변화를 쌓아 **나만의 movement portfolio를 만드는 개인 라이프 트래커**입니다. 광고와 로그인 없이 일상 기록은 가볍게, 원본 트래킹 데이터는 기기에 보존합니다.

## MVP

- 매일 기록 알림
- 몸무게 기록
- 운동 시간 / 거리 기록
- 예상 소모 칼로리 자동 계산 및 저장
- 날짜별 기록 저장
- 몸무게 / 운동량 / 소모 칼로리 추이 그래프
- 시스템 다크/라이트 모드 대응
- 로컬 우선 저장(SQLite), 계정/서버 없음
- GPS 운동 시작·일시정지·재개·종료, 경로/통계 이력, 결과 이미지 공유

## Product principles

1. **Fast** — 앱을 열고 30초 안에 오늘 기록을 끝낼 수 있어야 합니다.
2. **Light** — 광고, 피드, 소셜, 계정 시스템을 넣지 않습니다.
3. **Private** — 기본 데이터는 사용자의 기기에만 저장합니다.
4. **Calm** — 자극적인 연속 출석/죄책감 UX 대신 추세와 꾸준함을 보여줍니다.

## Planned stack

- Flutter / Dart
- `sqflite` — 로컬 기록 저장
- `flutter_local_notifications` — 매일 알림
- `fl_chart` — 추이 그래프
- `shared_preferences` — 알림 시간 등 간단한 설정
- `geolocator` 14.0.3 — GPS 및 Android foreground 위치 서비스
- `share_plus` 13.3.0 — 시스템 공유창으로 PNG 카드 공유
- `flutter_map` 8.3.2 / `latlong2` — 실제 지도, 경로, 위치 추적 및 공유용 지도 캡처

## Calorie estimate

예상 운동 소모 칼로리는 MET 기반 공식을 사용합니다.

`kcal = MET × 3.5 × 체중(kg) ÷ 200 × 운동시간(분)`

실제 소모량은 속도, 경사, 체성분, 심박수 등에 따라 달라질 수 있으므로 앱에서는 **예상치**로 표시합니다.

## 구현 및 실행

Flutter 표준 Android/iOS 프로젝트입니다. Flutter **3.47.2 / Dart 3.13.2**로 개발·검증합니다.
Android applicationId는 `com.v4n1lla.diligent_life`입니다.

```sh
flutter pub get
flutter run
flutter analyze
flutter test
dart format --output=none --set-exit-if-changed .
flutter build apk --debug
```

이 작업 환경의 SDK는 Git에서 제외한 `.tools/flutter`에 있습니다. PowerShell에서:

```powershell
$env:PATH = "$PWD\.tools\flutter\bin;$env:PATH"
$env:PUB_CACHE = "$PWD\.tools\pub-cache"
flutter run
```

Android SDK와 JDK 17을 준비하고 `flutter doctor -v`로 확인하세요.
iOS 실행·빌드는 macOS와 Xcode가 필요합니다. iOS 표준 타깃과 알림 권한 흐름은 포함되어 있으나 iOS 기기 검증은 별도로 필요합니다.
릴리스 배포 전에는 Android의 기본 개발용 서명을 배포용 키로 교체해야 합니다.

## 화면과 기록 정책

- **오늘**: 날짜, 몸무게, 운동 프리셋, 시간, 선택 거리 입력. 예상 소모 칼로리 즉시 표시 및 날짜별 저장/수정.
- **포트폴리오**: 최근 30일(오늘 포함) / 올해 / 전체의 완료 GPS 운동 합계, 실제 몸무게 측정 그래프, 월별 운동량, 가장 멀리 간 운동·가장 빠른 이동 구간과 대표 경로 지도.
- **설정**: 매일 기록 알림 ON/OFF, 시간(기본 20:00), 앱 정보, 오픈소스 라이선스.
- 몸무게만 기록할 수 있습니다. 운동 시간은 0분으로 저장됩니다.
- 몸무게를 비우면 선택 날짜 이전의 최근 측정값으로 칼로리를 계산합니다. 과거 날짜를 기록할 때 미래 측정값은 사용하지 않습니다.
- 대체 사용한 몸무게는 새 측정값으로 저장하지 않습니다. 따라서 측정하지 않은 날의 몸무게 추이를 만들지 않습니다.
- 오늘 화면의 수동 운동 기록은 칼로리 계산을 위해 몸무게 입력을 안내합니다. GPS 운동은 몸무게 없이도 경로를 저장하며 kcal를 표시하지 않습니다.
- 거리를 입력하면 운동 시간도 필요합니다. 거리는 칼로리 공식에 사용하지 않습니다.
- 저장된 칼로리는 해당 저장 시점의 추정치입니다. 과거 몸무게 수정으로 다른 날짜의 칼로리를 소급 변경하지 않습니다.
- 그래프는 실제 달력 날짜 간격을 사용합니다. 기록이 없는 날짜를 0으로 채우지 않습니다.
- 오류가 발생하면 재시도할 수 있으며 저장 실패 시 입력값은 유지됩니다.

## 주요 파일

```text
lib/
  main.dart                       앱 시작, NavigationBar, 한국어/테마 설정
  models/daily_record.dart         SQLite 레코드 변환
  models/exercise_type.dart        운동 프리셋 및 MET
  data/record_repository.dart      SQLite 생성, 날짜 조회, 트랜잭션 저장
  services/reminder_service.dart   알림 권한·예약·설정 저장
  screens/today_screen.dart        빠른 입력과 날짜별 수정
  screens/trends_screen.dart       fl_chart 그래프 및 기간 필터
  screens/settings_screen.dart     최소 설정
  theme/app_theme.dart             차분한 Material 3 Light/Dark 테마
  utils/calories.dart              독립적인 칼로리 계산
  utils/dates.dart                 지역 날짜 정규화
 test/                            계산, 실제 SQLite, 알림 시간, 화면 테스트
```

## SQLite 구조

파일: `diligent_life.db`, 스키마 버전: 3. 기존 `daily_records`와 운동/경로를 유지하며 원본 GPS 표본 테이블을 추가합니다. 아래 표는 일상 기록 구조이며, 운동 데이터의 상세 구조는 [v0.3.0 기록](docs/movement-v0.3.0.md)에 있습니다.

| 필드 | SQLite 타입 | 정책 |
| --- | --- | --- |
| id | INTEGER | PRIMARY KEY AUTOINCREMENT |
| date | TEXT | NOT NULL UNIQUE, 지역 날짜 YYYY-MM-DD |
| weightKg | REAL | nullable, 실제 입력한 측정값, > 0 |
| exerciseType | TEXT | NOT NULL, enum 식별자 |
| durationMinutes | INTEGER | NOT NULL, >= 0 |
| distanceKm | REAL | nullable, >= 0 |
| estimatedCalories | REAL | NOT NULL, >= 0, 반올림 전 값 |
| createdAt | TEXT | NOT NULL, UTC ISO 8601 |
| updatedAt | TEXT | NOT NULL, UTC ISO 8601 |

날짜 UNIQUE 인덱스로 빠르게 조회합니다. 트랜잭션 안에서 해당 날짜를 먼저 UPDATE하고 없을 때 INSERT합니다. 수정 시 `id`, `createdAt`을 보존합니다.
알림 설정은 `shared_preferences`에 `reminder_enabled`, `reminder_hour`, `reminder_minute`로 저장합니다. `reminder_timezone`은 예약한 시간대를 기억해 불필요한 재예약을 방지합니다.
앱 자체 서버·계정·분석 SDK는 없습니다. v0.3.0부터 실제 지도 표시를 위해 INTERNET 권한을 사용하고 OpenStreetMap 지도 타일을 요청합니다. 지도 제공자는 요청한 지도 영역과 IP 주소를 알 수 있습니다. 원본 GPS·몸무게·운동 통계를 업로드하지 않습니다. 네트워크가 없어도 GPS 기록과 SQLite 저장은 계속되며, 미캐시 지도 로딩과 지도 이미지 공유에는 연결이 필요합니다.
Android 자동 클라우드 백업 및 기기 이전에서 로컬 데이터를 제외합니다. iOS OS 백업 정책은 iOS 출시 전에 별도 확인해야 합니다. 앱 삭제 시 복원 기능은 제공하지 않습니다.

## MET와 예상 칼로리

`kcal = MET × 3.5 × weightKg / 200 × durationMinutes`

| 프리셋 | 기본 MET |
| --- | ---: |
| 가벼운 걷기 | 2.8 |
| 빠른 걷기 | 4.3 |
| 조깅 | 7.0 |
| 달리기 | 9.8 |
| 기타 | 3.0 |

MVP의 고정 강도 가정입니다. 특히 기타는 모든 운동을 정확히 나타내지 않습니다. 70kg, 빠른 걷기 30분은 158.025kcal이며 화면에는 약 158kcal로 표시합니다. 몸무게와 강도는 개인별 실제 소모량을 보장하지 않으므로 항상 **예상 소모 칼로리**로 표시합니다.

## 알림 구현

[`flutter_local_notifications` 공식 설정](https://pub.dev/packages/flutter_local_notifications)에 따라 Android desugaring, 알림 아이콘, 수신기와 권한을 구성했습니다.

- 기본 OFF. 사용자가 켤 때만 Android 13+ POST_NOTIFICATIONS 및 iOS 알림 권한을 요청합니다.
- 권한이 거부되면 OFF를 유지하고 기기 설정 안내를 표시합니다.
- timezone DB와 `flutter_timezone`의 실제 기기 시간대로 다음 알림을 계산합니다.
- `zonedSchedule`, 고정 ID 1, `DateTimeComponents.time`으로 하루 한 번 예약합니다. 시간 변경 시 같은 ID를 갱신하고 OFF 시 취소합니다.
- Android는 `inexactAllowWhileIdle`을 사용합니다. 별도의 정확한 알람 권한은 필요하지 않으며 OS 절전으로 선택 시간보다 늦게 도착할 수 있습니다.
- BOOT_COMPLETED/MY_PACKAGE_REPLACED 수신기로 재부팅·앱 업데이트 후 예약을 복원합니다.
- 앱 재진입 시 시간대 및 권한을 확인합니다. 동일 시간대에 예약이 남아 있으면 유지하고, 예약이 없거나 시간대가 달라지면 갱신합니다. 앱이 종료된 동안 시간대를 바꾸면 다음 앱 실행 전에는 기존 시간대를 기준으로 알림이 올 수 있습니다.
- 문구: **오늘의 기록을 남겨볼까요?**

## 검증과 직접 확인할 항목

자동 검증은 `flutter analyze`, `flutter test`, Dart format 확인입니다. SQLite 테스트는 인메모리 SQLite 엔진을 실제로 사용하며 화면 테스트는 메모리 저장소로 UI 동작을 격리합니다. GitHub Actions에는 동일 검증과 Android debug APK 빌드가 포함되어 있습니다. 아직 push하지 않았으므로 CI 실행 결과는 없습니다.

초기 구현 시점의 2026-09-09 검증 결과: `flutter analyze` **No issues found**, `flutter test` **17개 모두 통과**, `dart format --output=none --set-exit-if-changed .` **변경 없음**, `git diff --check` **통과**. 360px 다크 모드에서 단일 데이터 그래프 및 1.5배 글꼴에서 저장 동작도 위젯 테스트로 확인했습니다.

아래는 초기 MVP 검증 당시의 체크리스트입니다. 최신 Android 실기기 결과는 [v0.3.0 검증 기록](docs/movement-v0.3.0.md)을 확인하세요.

- 앱 설치, 콜드 스타트, 키보드/뒤로가기, 한 손 30초 입력, 실제 Light/Dark 화면 및 큰 글꼴
- 종료 후 재실행 시 SQLite 기록 유지, 과거 날짜 수정, 7일/30일 경계
- Android 12 이하와 13+에서 알림 허용/거부/재허용, 시간 변경, OFF 후 취소
- 실제 선택 시각 알림, 절전, 재부팅, 앱 업데이트, 시간대/서머타임 변경
- Android 릴리스 빌드의 아이콘 유지 및 배포 서명, iOS 빌드/권한/알림

## 이후 개선 후보

우선 실제 사용자 입력 시간을 측정하고 간격·키보드 동선을 다듬습니다. 필요성이 확인되면 사용자가 직접 관리하는 파일 내보내기/가져오기, 기록 삭제를 추가할 수 있습니다. 계정, 클라우드 동기화, 서버, 광고, 소셜, 결제, 건강 플랫폼 연동은 현재 범위에 포함하지 않습니다.


## MVP v0.1.0 1차 품질 검수

기능 추가 없이 입력·그래프·알림 동작을 보완했습니다. 당시 버전은 `0.1.0+1`입니다.

- 예상 칼로리와 저장 버튼을 하단에 유지합니다. 숫자 키보드, 다음/완료 포커스, 운동 선택 후 시간 입력, 탭 전환 시 키보드 해제를 검증했습니다.
- 잘못된 숫자는 미리보기에도 적용하지 않으며 0분은 약 0kcal로 표시합니다. 검증 실패 시 해당 필드로 스크롤합니다.
- 알림 권한 요청은 OFF → ON에 한정합니다. 시간 변경은 같은 ID를 갱신하고 앱 재진입은 기존 대기 예약을 보존합니다.
- 날짜 조회 중 오래된 비동기 응답은 무시합니다. 이전 날짜 계산은 24시간 차감 대신 달력 날짜를 사용합니다.
- 날짜 공백은 그래프의 선을 끊습니다. 운동 0분은 실제 점으로 유지합니다. 날짜 라벨은 양 끝만 표시하고 Y축 눈금은 단위를 고려합니다. 몸무게 축은 최소 2kg 범위를 확보합니다.
- 저장소에서도 잘못된 날짜, NaN/Infinity, 음수 및 허용 범위를 벗어난 숫자를 거부합니다. 날짜 수정 시 id/createdAt 보존 정책은 동일합니다.

MET 값은 한 곳(`exercise_type.dart`)에 유지했습니다. [2024 Compendium의 걷기](https://pacompendium.com/walking/) 및 [달리기](https://pacompendium.com/running/) 강도 범위와 비교할 때 기존 값은 대표 강도 근삿값으로 사용할 수 있는 수준입니다. 최신 표의 정확한 값을 그대로 옮긴 것은 아니며, 속도나 경사로 실제 강도를 결정하지 않습니다. 기타의 3.0 MET는 가벼운 활동 가정임을 UI에 명시했습니다.

세부 검수 결과와 실기기 미검증 항목은 [품질 검수 기록](docs/quality-review-v0.1.0.md)에 정리합니다.

## v0.2.0 GPS 운동 기록

상단 운동 버튼에서 GPS 운동을 기록하고 지난 경로/통계를 조회합니다. 결과 이미지 공유에는 시작·도착 주변 200m 숨김이 기본 적용됩니다.
저장 구조, Android 백그라운드 동작 범위 및 실기기 확인 항목은 [GPS 구현 기록](docs/gps-v0.2.0.md)에 정리합니다.


## v0.3.0 지도와 movement portfolio

- 운동 중 실제 지도, 최근 유효 위치 마커, 속도별 경로, 기본 위치 추적과 재중앙 버튼.
- 지도 아래 큰 수치와 고정 일시정지/재개/종료 버튼. 기존 이탈 보호 유지.
- 개인 상세는 전체 경로, 공유 미리보기는 시작·도착 200m 숨김 기본 적용.
- 약 5초 이상 시간 구간의 거리/시간으로 속도·페이스를 분석하고 선택 위치를 지도에 표시.
- 실제 지도와 경로·핵심 수치를 담은 PNG 카드와 Android 시스템 공유창.
- 완료한 GPS 운동의 누적 횟수·거리·시간·예상 kcal. 월/연 단위 경계로 집계 가능한 `MovementSummary`.
- SQLite v3: 기존 경로·일상 기록 보존, raw 센서 표본·제외 사유·필터 버전·누적 거리 추가.

설계, 지도 제공자 정책, 원본 보존 범위, 실기기 검증은 [v0.3.0 구현 기록](docs/movement-v0.3.0.md)에 정리합니다. 월간/연간 화면, 누적 경로 지도, 몸무게와 GPS 운동의 통합 추이는 후속 확장 범위입니다.


## v0.4.0 포트폴리오

기존 추이 탭을 포트폴리오로 확장합니다. 거리·횟수·시간·예상 kcal를 큰 수치로 보여주고 몸무게, 월별 요약, 대표 기록과 지도는 구분선으로 정리합니다. 새 서버·로그인·dependency·DB 마이그레이션은 없습니다.

- 기간과 월별 집계는 기기의 지역 날짜 및 운동 **시작일** 기준입니다. 선택 기간 밖·미완료·미래 날짜 운동은 제외하며, 수동 운동을 GPS 운동과 중복 합산하지 않습니다.
- kcal를 계산할 체중 snapshot이 없는 운동은 0으로 처리하지 않고 계산 가능한 기록 수를 안내합니다. 몸무게 그래프는 측정값만 사용하며 측정일 사이 공백을 이어 그리지 않습니다.
- 가장 빠른 구간은 원본 GPS의 accepted 표본에서 최소 5초 구간 평균을 구하고 운동별 속도 상한을 적용합니다. 과거 원본이 없는 운동은 저장된 경로를 사용합니다. raw speed 순간 최댓값으로 대표 기록을 선정하지 않습니다.
- 지도는 선택 기간에서 경로가 있는 최장 거리 운동을 대표로 표시합니다. 모든 운동을 연결해 하나의 가상 경로를 만들지 않습니다. 대표 기록에서 기존 상세·공유·삭제로 이동할 수 있고 삭제 후 포트폴리오를 갱신합니다.
- `lib/data/portfolio_repository.dart`에서 기존 SQLite 조회와 집계를 조합합니다. raw 경로는 세션별로 읽고, 캐시에는 세션별 대표 속도 구간만 보관합니다. 기본 월별 목록은 최근 6개월이며 더 보기로 확장합니다.

## v0.5.0 오래 쌓는 개인 포트폴리오

- 월간(기본)·연간은 이전/다음 기간을 탐색하고, 최근 30일·전체도 선택할 수 있습니다. 운동 시작일의 지역 날짜 기준으로 횟수·거리·시간·예상 kcal·실제 몸무게 변화와 월별 거리 막대를 표시합니다.
- 최장 거리·최장 시간·최고 평균속도/페이스·최소 5초의 가장 빠른 구간과 최근 5개 운동에서 상세로 이동합니다. 평균속도는 일시정지를 제외한 전체 운동 시간 기준이고, 20m 미만/0초/운동별 속도 상한 초과는 대표 평균속도에서 제외합니다.
- All-time Map은 기간 필터와 별도로 완료된 모든 운동의 경로를 모읍니다. 운동 사이와 GPS 공백은 연결하지 않으며, 표시용 경로만 간략화합니다. 최초 분석은 1,000행씩 읽고, 이후에는 재생성 가능한 `portfolio_analysis` 캐시를 사용합니다. 원본 테이블과 DB 버전 3은 유지합니다. 지도는 약 50,000점을 운동별로 나누어 샘플링하되 구간 경계는 항상 보존하므로 공백이 매우 많으면 목표 점 수를 초과할 수 있습니다.
- 월간/연간 요약은 900×1200 PNG 미리보기 후 시스템 공유창을 엽니다. 몸무게 변화와 월별 활동량을 포함하고 위치 경로는 포함하지 않습니다. 공유 대상과 전송은 사용자가 선택합니다.
- 설정의 **기록 백업과 복원**에서 기기의 파일 선택기로 `.diligent` 파일을 저장/선택합니다. 서버·계정·클라우드 SDK나 새 패키지를 추가하지 않았습니다. Android는 로컬 문서만 선택하도록 요청하며 iOS에서는 사용자가 기기 내 폴더를 선택합니다.
- 백업은 버전 헤더와 완료 행/행 수를 가진 스트리밍 NDJSON입니다. `daily_records`, `exercise_sessions`, `route_points`, `raw_route_points`의 모든 열과 ID, 측정값, 체중 snapshot, segment, raw speed, 수신 시각, 제외 사유, 필터 버전을 포함합니다. 파생 캐시·지도 타일·알림 설정은 제외합니다. 숨기지 않은 위치와 몸무게를 포함하므로 개인 로컬 폴더에 보관합니다.
- 가져오기는 임시 DB에서 형식·관계·숫자·행 수를 검증하고 기록 수를 미리 보여준 뒤 **명시적 확인 시 전체 교체**합니다. 병합하지 않습니다. 현재 운동과 미저장 입력이 있으면 차단하며, 실패 시 트랜잭션을 롤백합니다. 먼저 현재 기록을 내보내야 이전 상태로 돌아갈 수 있습니다. 백업의 미완료 운동은 일시정지로 복원합니다.
- 원본 GPS를 삭제하거나 재작성하지 않습니다. 전체 교체는 사용자가 확인한 복원 작업에서만 수행하며, 이후 원본으로 속도와 표시 경로를 다시 분석할 수 있습니다.

v0.5.0 검증(2026-09-14): `dart format .`, `flutter analyze`, `flutter test` **88개 통과**, Android debug APK 빌드 성공. 날짜 경계·대표 기록·12,005개 표본의 스트리밍 분석/샘플링·지도 구간 분리·PNG 생성·전체 백업 왕복·손상 파일 거부·원자적 롤백·가져오기 확인/취소를 포함합니다.

SM-F936N에 0.5.0+6을 설치하고 월간/연간/전체 합계, 몸무게 그래프, 월별 요약, 대표/최근 운동 상세 이동, 누적 경로, 이미지 카드와 시스템 공유창 열기/취소, 로컬 파일 저장·가져오기 확인/취소·전체 복원을 확인했습니다. 합성 운동 5개·경로 1,505개·raw 1,510개를 다시 내보낸 결과 모든 원본 열이 동일했습니다. 이후 검증 전의 빈 DB로 복원하고 임시 합성 파일을 제거했습니다. 기기의 `tile.openstreetmap.org` DNS 조회 실패로 배경 지도 타일 로딩은 확인하지 못했고 경로 표시와 오류/재시도 안내를 확인했습니다. iOS 파일 선택기 구현은 포함하지만 Windows 환경에서 iOS 빌드·실기기 검증은 하지 못했습니다. 기존 flutter_timezone의 KGP 전환 예고 경고는 빌드를 차단하지 않습니다.


## v0.6.0 GPS 움직임 재분석

- 운동 상세에 유효 이동거리, 이동/자동 정지/미분류 시간, 평균 이동속도, 최고 유효 구간속도, 가장 빠른 100m/500m/1km를 표시합니다. 시간–속도 그래프와 지도는 같은 색상 기준을 사용하며 선택한 구간을 지도에서 강조합니다.
- `MovementAnalyzer`는 raw의 과거 accepted/rejected 결과에 의존하지 않고 좌표·정확도·시각을 다시 검사합니다. 수동 재개의 `before_segment`, 정확도 30m 초과, 중복/역행 시각, 오래되거나 미래인 수신, 비현실적 점프를 제외합니다. 수동 일시정지와 15초 초과 수신 공백을 연결하지 않습니다.
- 3개 연속 좌표의 중앙값으로 잡음을 줄이고 약 6초 구간의 좌표 거리/시간을 계산합니다. 정확도 반경 안의 느린 이동은 최대 약 12초까지 관찰합니다. 센서 `rawSpeed` 순간값은 분석에 사용하지 않습니다. 구간 평균 상한은 가벼운 걷기 3m/s, 빠른 걷기 3.5m/s, 조깅 5m/s, 달리기/기타 12m/s입니다. 이 기준은 보수적인 경험값이며 느린 차량과 운동을 완전히 구별하는 분류기는 아닙니다.
- 정확도에 따른 3–8m 반경과 걷기 0.45m/s·그 외 0.6m/s 이동 문턱을 사용합니다. 연속 정지 증거가 10초 이상이면 자동 정지로 합칩니다(1Hz, 6초 창에서는 통상 12초부터). 정지의 흔들림은 거리에서 제외하고 재출발 시 누적해 연결하지 않습니다. 신호 공백, 제외 구간, 짧거나 불확실한 구간은 미분류로 남깁니다. 수동 일시정지는 원래 기록 시간에 포함되지 않으며 그래프 가로축은 실제 기록 시작 후 시간입니다. 전환 경계는 창 길이만큼의 오차가 있을 수 있습니다.
- 빠른 거리 기록은 끊기지 않은 유효 이동에서 누적 거리의 양 끝을 보간합니다. 시작 또는 끝이 각 거리 표본에 놓이는 후보를 모두 검사해 세 거리마다 O(n)으로 구합니다. 정지·제외 구간을 가로지르지 않습니다.
- 원본 GPS가 없는 과거 운동은 저장된 경로로 제한적으로 분석하고, 정지 시간은 복원하지 않습니다. 새 결과는 버전별 파생 캐시에만 저장합니다. 원본 GPS·운동 세션·기록 당시 합계와 공유 수치를 수정하지 않으며, 상세의 ‘원본으로 다시 계산’으로 재생성할 수 있습니다. 포트폴리오의 구간 최고속도와 표시 경로도 새 분석을 사용합니다.
- 원본은 1,000행씩 읽고 최고 기록 계산은 별도 isolate에서 수행합니다. 정확한 기록 계산 후에만 각 구간의 지도용 좌표를 최대 3개로 줄이고 포트폴리오 표시 경로를 추가 샘플링합니다. 전체 백업은 기존 원본 네 테이블을 보존하고 복원 후 파생 캐시를 다시 만듭니다. 새 dependency나 DB 스키마 버전 변경은 없습니다.

v0.6.0 검증(2026-09-15): `dart format .`, `flutter analyze` 통과, `flutter test` **106개 통과**, `flutter build apk --debug` 성공. 정지→걷기→정지, 정지 잡음, 느린 걷기/낮은 정확도, 활동별 과속, GPS 튐과 재수신, 공백/수동 정지, 거리 보간, 24,001개 raw 표본, 캐시 재계산과 원본 열 보존, 백업/복원, 320px·2배 글자·밝은/어두운 테마, 그래프와 지도 선택 연동을 포함합니다. SM-G988N의 USB 디버깅 승인 후 v0.6.0+7을 설치하고 기존 운동의 분석 수치, 거리별 최고 기록, 속도 그래프 선택·지도 연결, 지도 타일 로딩과 원본 재계산을 확인했습니다. 안내 문구 한 줄의 깨진 한글을 수정했습니다. 기존 기록을 추가·삭제하지 않았으며, 실제 정지→걷기→정지 실험은 사용자가 진행하지 않아 미검증입니다. 기존 flutter_timezone KGP 전환 예고 및 Android SDK XML 버전 경고는 빌드를 차단하지 않았습니다.

추가 모의 보행 검증: 1Hz 합성 GPS로 정지 120초 → 1.4m/s 걷기 600초 → 정지 90초 → 걷기 300초를 재생했습니다. 이동 900초·정지 210초·1,260m·평균 5.04km/h, 빠른 100m 71.43초·500m 357.14초로 예상값과 일치했습니다. ±0.5m 잡음에서는 이동·정지 시간이 같고 거리는 1,259m였습니다. 5km 순간 튐과 30초간 20m/s 이동을 추가한 경우 32개 표본을 제외하고 이동 858초·정지 210초·미분류 42초·1,199.8m로 계산됐으며, 최고 유효속도와 빠른 구간에 이상 속도가 반영되지 않았습니다. 합성 표본은 휴대폰 DB에 넣지 않았습니다. 이는 실제 보행 실험을 대체하는 현장 정확도 검증이 아니며, 실제 보행 미검증 상태를 명시하고 사용자의 승인으로 커밋합니다.

## v0.7.0 활동 리포트

- 포트폴리오의 ‘활동 리포트’에서 주간(월요일 시작), 월간, 연간을 이동합니다. 완료 GPS 운동의 기록 당시 거리·시간·횟수·예상 kcal를 이전 기간과 비교하며 수동 입력 운동은 중복 합산하지 않습니다. 진행 중인 기간은 오늘까지와 이전 기간의 같은 경과 일수를 비교합니다. 이전 기간이 짧으면 그 마지막 날까지만 비교하며 실제 양쪽 날짜를 화면과 공유 카드에 표시합니다. 완료된 기간은 각각 전체 기간을 비교하며 월별 일수 차이를 정규화하지 않습니다.
- 증감률은 양쪽 모두 2회 이상일 때 `(이번−이전)/이전×100`으로 계산합니다. 이전 값이 0이면 백분율을 만들지 않고 계산 불가로 표시합니다. kcal는 각 기간의 계산 가능 횟수와 부분 합계를 표시하고, 어느 쪽이든 몸무게 누락이 있으면 증감률을 내지 않습니다.
- 평균 이동속도·페이스는 v0.6.0의 유효 이동거리/이동시간을 운동 종류별로 합산합니다. 각 운동은 유효 이동 100m·60초 이상, 각 비교 기간/주에는 해당 종류의 운동 2회 이상이 필요합니다. 선택 기간 종료일 기준 최근 완료된 4개의 월요일–일요일 주를 비교하며, 매주 0.05km/h보다 크게 증가한 경우만 점진적 상승 문구를 만듭니다. 원본이 없거나 유효 분석이 부족한 경우 ‘데이터 부족’으로 남깁니다.
- 몸무게는 기간 내 실제 첫/마지막 측정의 변화와 일별(주간), 7일 구간별(월간), 월별(연간) 측정 평균을 운동량과 나란히 표시합니다. 측정이 없는 칸을 보간하지 않습니다. 상관계수나 인과관계, 건강 평가·조언은 만들지 않습니다.
- 요일·시작 시간대는 현지 시각의 빈도입니다. 운동 4회·서로 다른 날짜 3개 이상부터 표시하고 동률을 모두 표시합니다. 특정 ‘요일+시간대’ 문구는 그 조합이 유일한 최빈값이며 3회 이상일 때만 표시합니다. 별도로 계산한 요일과 시간대의 최빈값을 임의로 합치지 않습니다.
- 유사 경로는 표시용 샘플(목표 64점)의 지구 중심 좌표를 약 100m 격자로 나눈 겹침 추정입니다. 300m·8점·4격자 이상인 경로끼리, 대표 경로와 거리 비율 0.75–1.33, 양쪽 격자 각각 70% 이상 겹쳐야 묶습니다. 반대 방향은 구분하지 않습니다. 실제 도로 식별이 아니므로 인접·일부 공통 경로가 묶이거나 샘플링/격자 경계로 유사 경로가 누락될 수 있습니다. 최대 3개 반복 그룹의 대표 운동을 열 수 있습니다.
- 개인 최고는 선택 기간 마지막 날까지의 최장 거리/시간, 운동 종류별 평균 이동속도와 빠른 100m/500m/1km입니다. 시간순으로 이전 최고를 넘은 기록만 최근 개선으로 표시하며 최초 기록은 갱신으로 세지 않습니다. 대표 운동은 유효 이동거리, 기록 시간, ID 순으로 결정하고 상세 화면으로 연결합니다.
- 주간·월간·연간 이미지는 동일한 계산 결과와 비교 날짜·몸무게 변화·종류별 속도를 사용합니다. 기존 미리보기/시스템 공유를 재사용하며 경로 좌표·요일/시간대 패턴은 이미지에 포함하지 않습니다. 화면에서 공유 전에 몸무게 포함 사실을 안내합니다.
- SQLite의 기존 `portfolio_analysis`를 재사용합니다. 리포트 화면에는 가벼운 세션별 요약만 보관하고 새로고침/운동 상세 복귀 시 비웁니다. 유사 경로는 격자 역색인으로 후보를 좁히고 리포트 계산은 isolate에서 수행합니다. 원본 GPS와 DB 스키마를 바꾸지 않으며 새 dependency는 없습니다.

v0.7.0 검증(2026-09-15): `dart format .`, `flutter analyze`, `flutter test` **123개 통과**, `flutter build apk --debug` 성공. 기간 경계·윤년·미완료 기간·0 기준/누락 kcal·종류별 시간 가중 속도·4주 표본 부족·측정 공백·시간대 동률·반대 방향/다른 위치의 경로·개인 최고 갱신·삭제/캐시 갱신·3개 기간의 PNG 디코딩·320px/2배 글자·공유 미리보기 오류/재시도를 검증했습니다. SM-G988N에 0.7.0+8 설치와 시작 요청은 성공했으나 패턴 잠금이 해제되지 않아 실제 리포트 화면·기간 전환·시스템 공유창 조작은 미검증입니다.

v0.7.0 실기기 확인(2026-09-16): 잠금 해제된 SM-G988N의 기존 0.7.0+8에서 주간·월간·연간 리포트, 각 기간의 이전/다음 이동, 이전 기간 비교 날짜와 합계를 확인했습니다. 진행 중인 기간은 같은 경과 일수, 완료된 기간은 전체 기간으로 비교되며 이전 기록이 없는 경우 데이터 부족으로 표시됩니다. 세 기간 모두 리포트 이미지 생성·미리보기와 Android 시스템 공유창 열기를 확인하고 공유창을 닫았습니다. 실제 외부 전송이나 운동 기록 변경은 하지 않았습니다. 확인 범위에서 오류가 없어 앱 코드를 수정하지 않았으며, 사용자 요청에 따라 analyze/test/build는 재실행하지 않았습니다.

개발 검증 방침: 가능한 검증은 모의테스트를 우선합니다. 실기기 확인은 필요한 경우 사용자와 정한 범위에서 진행합니다.
